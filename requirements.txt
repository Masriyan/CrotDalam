# Runtime dependencies. Install the package itself with: pip install -e .
beautifulsoup4>=4.12
networkx>=3.0
regex>=2023.0
cryptography>=49.0.0

# Optional: PDF report export (crotdalam report --format pdf)
reportlab>=4.0

# Optional: test coverage and lint used by the QA workflow
coverage>=7.0
pyflakes>=3.0
pillow>=10.2.0 # not directly required, pinned by Snyk to avoid a vulnerability
